/* Edit this one line after you deploy your Worker (see README).
   This is the public database URL, not a secret — safe to commit.
   The family code is NOT here; each device enters it once and it stays local. */
window.GJJ_CONFIG = {
  apiBase: ""  // e.g. "https://gjj-api.your-name.workers.dev"
};
